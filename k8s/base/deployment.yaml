apiVersion: apps/v1
kind: Deployment
metadata:
  name: kustomizatoin-demo
  labels:
    app: kustomizatoin-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kustomizatoin-demo
  template:
    metadata:
      labels:
        app: kustomizatoin-demo
    spec:
      imagePullSecrets:
        - name: ecr-registry
      containers:
        - name: kustomizatoin-demo
          image: 456433063024.dkr.ecr.us-east-2.amazonaws.com/datascience/k8s-argocd-demo-app:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: "500m"    # reserve 0.5 CPU core
              memory: "256Mi"   # reserve 256 MiB RAM
            limits:
              cpu: "1"       # cap at 1 CPU core
              memory: "512Mi"   # cap at 512 MiB RAM

